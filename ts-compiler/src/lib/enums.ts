export enum TokenType {
  // Numerical
  INT = "INT",

  // Operations
  PLUS = "PLUS",
  MINUS = "MINUS",
  X = "X",
  DIVIDE = "DIVIDE",

  // EOF
  EOF = "EOF"
}

export enum TokenRepresentation {
  PLUS = "+",
  MINUS = "-",
  X = "*",
  DIVIDE = "/",
}
